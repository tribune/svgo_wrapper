require 'spec_helper'

describe Tribune::SvgoWrapper do
  it 'has a version number' do
    expect(Tribune::SvgoWrapper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
